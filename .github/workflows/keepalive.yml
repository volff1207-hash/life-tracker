name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Life Tracker
        run: curl -s https://life-tracker-nm1r.onrender.com/api/settings > /dev/null && echo "OK"
